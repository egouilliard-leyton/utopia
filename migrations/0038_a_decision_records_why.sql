-- 0026：一次裁决记下的是「为什么」，不只是「是什么」。
--
-- `reason` 一列早就有，但它记的是这一对**为什么被排进队列**（namesake、
-- escalate_unsure……），由排队的代码写。人拍板时想的那句话——"同一个地址"
-- "一个是子公司"——没有地方落。于是裁决器和治理 agent 读先例时只看得见
-- 结果，看不见依据：一次错误的合并会被当成"这类该合"的证据，错误洗成政策。
-- 这一列记人写的那一句；空着是允许的，问的是「什么让你这么定」，不是一张表。
ALTER TABLE resolution_reviews ADD COLUMN rationale TEXT;
