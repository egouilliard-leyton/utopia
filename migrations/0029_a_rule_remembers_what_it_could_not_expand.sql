-- 0021 打磨：一条规则上次跑的时候，有几个实体的前提组合没展开完。
--
-- **少推几条与「这个实体不满足」在结果里长得一模一样。** 这个数字此前只出现在
-- 「立刻跑一遍」那一刻的提示里，看过就没了——而规则卡片是人回头判断「它到底
-- 靠不靠得住」的地方。落一列，物化每轮写回，卡片上常驻。
ALTER TABLE attribute_rules
    ADD COLUMN capped_at_last_run INT NOT NULL DEFAULT 0;
