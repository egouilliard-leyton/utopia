-- 抽取只写开放图谱（0044 决定 2）：开关退场。
--
-- 0061 加这一列时给带本体的老路留了一个关掉的口子——按当时的决定 3，它是已批准本体下
-- 的可选第二路，等对齐追平就退场。现在定了：没有遗留层，也没有可选的类型化第二路。
-- 每篇文档（记忆日志也在内）都走 `extraction_open::run_open`，本体不进提示词；类型化的
-- 事实由对齐从开放陈述算出来，不由抽取写。一个再也没有第二个取值的开关不该留在表里：
-- 留着，读的人会以为还有一条路可以切回去。
--
-- 没有已发布的版本，直接删列；已有的 `layer = 'typed'` 行不动，它们是对齐的输入。
ALTER TABLE knowledge_bases DROP COLUMN open_extraction;
